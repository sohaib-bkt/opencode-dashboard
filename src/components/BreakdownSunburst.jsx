export default function BreakdownSunburst({ breakdown, onCategorySelect }) {
  return (
    <div style={{ color: "var(--text-secondary)", fontSize: "14px" }}>
      BreakdownSunburst (stub)
    </div>
  );
}
