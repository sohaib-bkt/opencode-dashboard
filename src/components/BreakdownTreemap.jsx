export default function BreakdownTreemap({ breakdown, onCategorySelect }) {
  return (
    <div style={{ color: "var(--text-secondary)", fontSize: "14px" }}>
      BreakdownTreemap (stub)
    </div>
  );
}
